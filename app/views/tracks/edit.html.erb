<%= my_form(
            @track,
            :update,
            [:name, :text],
            [:bonus_or_regular, :radio],
            [:lyrics, :textarea],
            [:album_id, :dropdown]
            ) %>
