<%= my_form(
            @track,
            :create,
            [:name, :text],
            [:bonus_or_regular, :radio],
            [:lyrics, :textarea],
            [:album_id, :dropdown]
          ) %>
